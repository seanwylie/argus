#!/usr/bin/env sh
# Argus scaffold: start hook for example-app.
# Local-safe: no network. External calls require capability approval (docs/execution.md).
set -eu
: "${ARGUS_PRODUCT_ID:=example-app}"
exit 0
