"""Strategic operating modes and deterministic product strategy snapshots.

Import submodules explicitly, e.g. ``from argus.strategy.snapshot import ...``,
to avoid import cycles at package load time.
"""
