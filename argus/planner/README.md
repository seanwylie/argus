# `argus.planner` (deprecated)

**This package is deprecated and retained only as a compatibility placeholder.**

- **Use [`argus.planning`](../planning/)** for all portfolio planning: weekly synthesis, planning-derived action contracts, and `runs/planning/` artifacts.
- **CLI:** `argus planning weekly`, `argus planning actions`, etc.

Do not add new modules here. New work belongs under `argus/planning/`.
