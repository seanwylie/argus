# lifecycle

**Intent:** Deterministic **lifecycle readiness scoring** for a product: five dimensions (`move_forward`, `hold`, `improve`, `deprecate`, `kill`) plus `kill_candidate`.

Implementation: `argus/lifecycle/scoring.py` (`assess_lifecycle`), `argus/lifecycle/model.py` (`LifecycleAssessment`).

See [Decisions and lifecycle](../../docs/decisions-and-lifecycle.md).
